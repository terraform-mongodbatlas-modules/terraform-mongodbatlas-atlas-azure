```release-note:breaking-change
variable/privatelink_byo_endpoint: Renames `privatelink_byoe_regions` to `privatelink_byo_endpoint` to represent the Atlas-side BYOE PrivateLink endpoint
```

```release-note:breaking-change
variable/privatelink_byo_endpoint: Changes type from `map(string)` to `map(object({ region }))` to future proof support for cross-region BYOE PrivateLink
```

```release-note:breaking-change
variable/privatelink_byo_service: Renames `privatelink_byoe` to `privatelink_byo_service` to represent the user-managed Azure private endpoint linked to Atlas
```
